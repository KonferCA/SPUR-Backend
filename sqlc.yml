version: "2"
sql:
  - engine: "postgresql"
    queries: ".sqlc/queries"
    schema: ".sqlc/migrations"
    gen:
      go:
        package: "db"
        out: "db"
        sql_package: "pgx/v5"
        overrides:
          - db_type: "uuid"
            go_type: "string"
